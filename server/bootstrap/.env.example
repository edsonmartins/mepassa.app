# MePassa Bootstrap Node Configuration

# P2P listening port (libp2p)
BOOTSTRAP_PORT=4001

# HTTP health check port
HEALTH_PORT=8000

# Seed for deterministic peer ID generation
# Change this for each bootstrap node instance
PEER_ID_SEED=bootstrap-1

# Directory for persistent storage
DATA_DIR=/app/data

# Log level: trace, debug, info, warn, error
RUST_LOG=info
